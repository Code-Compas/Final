from main import app

# Vercel Python runtime will use the variable `app` as a WSGI application if present.
# This file simply exposes the already-constructed Flask app from `main.py`.

# `app` is imported above; no further code is required here.
